/** 本地启动命令 — PLAN2 F5-2 / dev_docs §〇 / README。 */

// 一行起 Runner + Flask + Vite（故事由大厅选/建后激活、世界数据驱动播种，无需手填 config/故事路径）。
export const RUN_DRAMA_COMMAND = `./agent_world/drama_demo/scripts/ops/start_demo.sh`;

export const POLL_TIMEOUT_MESSAGE =
  "轮询超时（120 次 × 1.5s），NPC 响应未完成。请确认 Runner 仍在运行后重新发送台词。";
